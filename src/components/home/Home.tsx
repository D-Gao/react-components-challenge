import { PlayerDrawer } from "./PlayerDrawer";

const Home = () => {
  return (
    <PlayerDrawer>
      <button
        onClick={() => {
          console.log("sasa");
        }}
      >
        dsadas
      </button>
    </PlayerDrawer>
  );
};

export default Home;
